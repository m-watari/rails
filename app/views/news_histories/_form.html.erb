<%= form_with(model: news_history) do |form| %>
  <% if news_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_history.errors.count, "error") %> prohibited this news_history from being saved:</h2>

      <ul>
        <% news_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_field :detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
